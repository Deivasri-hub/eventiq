'use client'; import {useState} from 'react'; import Link from 'next/link'; import {api,auth} from '../../lib/api'; export default function SignIn(){const [email,setEmail]=useState('student@eventiq.demo'),[password,setPassword]=useState('student123'),[error,setError]=useState('');async function go(e){e.preventDefault();try{const x=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});auth.set(x);location.href=x.user.role==='organizer'?'/organizer':'/dashboard'}catch(x){setError(x.message)}}return <main className="min-h-screen grid place-items-center p-5 bg-soft"><form onSubmit={go} className="card p-8 w-full max-w-md"><div className="text-brand font-black text-xl">✦ EventIQ</div><h1 className="text-3xl font-black mt-8">Welcome back 👋</h1><p className="muted mt-2">Sign in to continue to EventIQ.</p><input className="input mt-7" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input mt-3" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/>{error&&<p className="text-red-600 text-sm mt-3">{error}</p>}<button className="btn btn-primary w-full mt-5">Sign In</button><p className="text-xs text-center mt-4">Demo: student@eventiq.demo / student123</p><p className="text-sm text-center mt-4">Don't have an account? <Link className="text-brand font-bold" href="/signup">Sign Up</Link></p></form></main>}
